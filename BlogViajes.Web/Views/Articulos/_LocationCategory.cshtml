@model List<BlogViajes.EntityModels.CategoryModel>
@if (Model.Count != 0)
{
  <ul>
    @foreach (var item in Model)
    {
        <li><a href="/Articulos/Destinos/0/@item.Id">@item.Name</a></li>
    }
</ul>  
}

